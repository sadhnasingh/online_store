<%= form_for :cart, url: carts_path  do |form| %>
	<%= form.text_field :quantity , :value=>"1" %>
	<span>Quantity</span>
	<%= form.hidden_field :product_id, :value=> @product.id %>
	<%= form.submit 'Add to Cart', :class => "bay" %>
	<!-- <a href="#" class="bay">Add to Cart</a>
	<input type="text" name="" class="number" value="1" />
	<span>Quantity:</span> -->
<% end%>